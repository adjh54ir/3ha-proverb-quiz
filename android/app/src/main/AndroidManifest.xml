<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

  <!--
    아래 권한들은 라이브러리(notifee 등) 매니페스트가 자동으로 넣지만 이 앱은 쓰지 않는다.
    - SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM : 리마인더는 '정확한 알람'이 필요 없다.
      (앱 실행마다 저장된 시각으로 재예약해 드리프트를 보정하므로 부정확 알람으로 충분하다)
      Android 14+ 에서는 사용자가 직접 허용해야 동작하고, USE_EXACT_ALARM 은
      알람/캘린더 앱이 아니면 Play 정책상 거절 사유가 된다.
    - ACCESS_NOTIFICATION_POLICY / BROADCAST_CLOSE_SYSTEM_DIALOGS : 방해금지 모드 제어·시스템
      다이얼로그 닫기를 하지 않는다.
    - FOREGROUND_SERVICE : 포그라운드 서비스 알림을 쓰지 않는다.
  -->
  <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" tools:node="remove" />
  <uses-permission android:name="android.permission.USE_EXACT_ALARM" tools:node="remove" />
  <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" tools:node="remove" />
  <uses-permission android:name="android.permission.BROADCAST_CLOSE_SYSTEM_DIALOGS" tools:node="remove" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" tools:node="remove" />

  <application
    android:name=".MainApplication"
    android:label="@string/app_name"
    android:icon="@mipmap/ic_launcher"
    android:roundIcon="@mipmap/ic_launcher"
    android:allowBackup="false"
    android:theme="@style/AppTheme"
    android:supportsRtl="true">
    <!--
      targetSdk 36(Android 16)부터는 최소 너비 600dp 이상 화면에서 시스템이
      android:screenOrientation 과 resizeableActivity 지정을 무시하고 앱을 강제로
      회전·리사이즈 가능하게 만든다. 이 앱은 세로 한 컬럼 설계라 그대로 두면
      태블릿에서 가로로 돌아가며 레이아웃이 깨진다.
      아래 속성으로 기존 세로 고정을 유지한다. targetSdk 37 부터는 무효가 되므로
      그때는 가로 대응 레이아웃이 필요하다.
    -->
    <property
      android:name="android.window.PROPERTY_COMPAT_ALLOW_RESTRICTED_RESIZABILITY"
      android:value="true" />
    <activity
      android:name=".MainActivity"
      android:label="@string/app_name"
      android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
      android:launchMode="singleTask"
      android:screenOrientation="portrait"
      android:windowSoftInputMode="adjustResize"
      android:exported="true"
      android:theme="@style/BootTheme">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>
</manifest>
